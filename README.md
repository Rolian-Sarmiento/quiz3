This is HAU Cleaning Services
This is front-end only.

It displays the services of my project which should contain:
An interactable logo that sends you to homescreen '/'
3 items that contain detail screens '/content/:id/'

To run this project,
open the terminal,
### `cd frontend` then
### `npm install` then
### `npm start`
